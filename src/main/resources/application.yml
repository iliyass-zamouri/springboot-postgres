server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5332/backend-db
    username: db_user
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet

